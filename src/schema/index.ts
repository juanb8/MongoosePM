import userSchema, { type UserModel } from "./user.schema";
import projectSchema, { type ProjectModel } from "./project.schema";

export { userSchema, projectSchema, type UserModel, type ProjectModel };
